projection;
strict ( 2 );
use draft;

define behavior for Zc_SALES_YJKIM_U //alias <alias_name>
{
  use create;
  use update;
  use delete;
  use action uploadexcel;
  use action getexchangerate;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}